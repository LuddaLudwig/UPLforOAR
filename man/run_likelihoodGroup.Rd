% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_likelihoodGroup.R
\name{run_likelihoodGroup}
\alias{run_likelihoodGroup}
\title{Runs JAGS model scripts for chosen likelihood with a hierarchical group structure}
\usage{
run_likelihoodGroup(
  model_input,
  xvals = NULL,
  minY = 0,
  maxY = NULL,
  future_runs = 3,
  group = "sources"
)
}
\arguments{
\item{model_input}{Results from \code{\link[=setup_likelihoodGroup]{setup_likelihoodGroup()}}, including JAGS model
script, emissions data, distribution, initial values list, and parameters to
monitor.}

\item{xvals}{Ordered sequence of emissions at which to predict probability
density. Default is \code{NULL}, in which case \code{x_hat} is a 1024 length
sequence between \code{0} and \code{3 * max(data$emissions)} or \code{minY} and \code{maxY} if
they are specified.}

\item{minY}{The minimum emission value possible, used to truncate likelihood
distributions. Default is 0.}

\item{maxY}{The maximum emission value possible, used to truncate likelihood
distributions and set upper ranges on prior distributions.
Default is \code{NULL}, in which case it is calculated as \code{3 * maximum(data$emissions)}.}

\item{future_runs}{Integer of future runs to use in prediction, the default
is \code{3} since compliance uses 1 test average of 3 runs.}

\item{group}{Character string corresponding to the variable name in the data
set by which to group for the hierarchical structure. If the group is not a
factor it will be coerced using as.factor(). To avoid having unknown factor
levels, please convert to factor first
(set using \code{as.factor(data$group_name)} if needed). Defaults to \code{'sources'}.}
}
\value{
\code{runjags} object named \code{run_results}, likelihood distribution from the
JAGS model script, as well as \code{data}, \code{group} and \code{xval}s used as inputs.
}
\description{
Runs the JAGS model from \code{\link[=setup_likelihood]{setup_likelihood()}} model_code output using \code{\link[runjags:run.jags]{runjags::run.jags()}}.
Traces all variables in \code{par_list} running 3 chains in parallel with \code{burnin} and
\code{adapt} of 10,000 each and keeping 10,000 iterations per chain. Rather than
independent runs defining the population distribution, hierarchical
dependency within groups is allowed with the
group-level distribution parameters drawn from the overall population distribution.
}
