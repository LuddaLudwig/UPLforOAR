% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_likelihoodGroup.R
\name{write_likelihoodGroup}
\alias{write_likelihoodGroup}
\title{Writes likelihood scripts for JAGS model calls with a hierarchical group structure}
\usage{
write_likelihoodGroup(distribution, manual_prior = FALSE, write_wd = NULL)
}
\arguments{
\item{distribution}{Any of \code{'Normal'}, \code{'Gamma'}, \code{'Skewed'}, \code{'Lognormal'},
or \code{'Beta'}.}

\item{manual_prior}{Default is \code{FALSE}, if priors should be specified
manually or be uninformative calculated from range of emissions data.}

\item{write_wd}{Default is \code{NULL}, in which case the JAGS scripts are written
into inst/JAGS folder in package directory. This is the location
\code{\link[=run_likelihoodGroup]{run_likelihoodGroup()}} will look for the JAGS scripts assigned via
\code{\link[=setup_likelihoodGroup]{setup_likelihoodGroup()}}.}
}
\value{
object \code{model_code}, which is a string for the written R script that
JAGS can call and the distribution used in likelihood model.
}
\description{
This function writes an R script for JAGS to call based on the selected
distribution and prior. The priors are uninformative and
set based on emissions data, unless specified manually via
\code{\link[=setup_likelihoodGroup]{setup_likelihoodGroup()}}. The likelihood distributions are truncated to
\verb{(minY, maxY)}, where \code{minY} and \code{maxY} can be specified or used with the
default \code{minY = 0} and \code{maxY = 3 * max(data$emissions)} in
\code{\link[=run_likelihood]{run_likelihood()}}. Rather than independent runs defining the population
distribution, hierarchical dependency within groups is allowed with the
group-level distribution parameters drawn from the overall population distribution.
}
