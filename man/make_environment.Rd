% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_environment.R
\name{make_environment}
\alias{make_environment}
\title{Makes a list of environments for automating analysis in reg text templates}
\usage{
make_environment(
  data,
  HAP,
  emissions,
  sources,
  subcat_name = NA,
  subcat_level = NA,
  type = c("New", "Existing", "As-is"),
  CAA_section = 112,
  national_N = NA,
  meas_unit,
  future_runs = 3,
  significance = 0.99,
  ...
)
}
\arguments{
\item{data}{A data set containing the sources, all emissions, and any
subcategories if applicable. Each row should be an individual run. Emissions
must be numeric, and sources and subcategories must be characters or factors.}

\item{HAP}{A string with the name of the Hazardous Air Pollutant (HAP) as it
should appear in text or figure labels.}

\item{emissions}{variable name or column number corresponding to the HAP
emissions used for selecting top performing sources.}

\item{sources}{variable name or column number corresponding to the source
designation, either a character or factor.}

\item{subcat_name}{The name of the subcategory variable corresponding to the HAP
given in \code{emissions}, either a character or factor. If there are no
subcategories for the HAP, use \code{NA}.}

\item{subcat_level}{If a subcategory is provided in \code{subcat_name}, indicate
which level here. For example, if the subcategory is \code{'facility_size'}, and
the levels are \code{c('small', 'medium', 'large')}, then one analysis might be
\code{subcat_level = 'medium'}. If there are no subcategories for the HAP, use \code{NA}.}

\item{type}{Either \code{'As-is'}, \code{'New'}, or \code{'Existing'}, Using \code{'As-is'} will
run the UPL analysis on the HAP emissions in \code{data} with no subsetting. \code{'New'}
will select the top performing source for the HAP using \code{\link[=MACT_new]{MACT_new()}}, and
\code{'Existing'} will select the top performing sources for the HAP using \code{\link[=MACT_existing]{MACT_existing()}}.}

\item{CAA_section}{Applicable Clean Air Act section, either 112 or 129.}

\item{national_N}{For Clean Air Act section 129 (\code{CAA_section = 129}), the
additional argument providing the total number of sources nation-wide
regardless of whether or not they contributed emissions data is required.}

\item{meas_unit}{String of measurment units to be used in text and figure axes labels.}

\item{future_runs}{Integer of future runs to use in prediction, the default
is \code{3} since compliance uses 1 test average of 3 runs.}

\item{significance}{Level of significance from 0 to 1, the default is \code{0.99}.}

\item{...}{Other arguments passed on to \code{\link[=Bayesian_UPL]{Bayesian_UPL()}} or \code{\link[=obs_density]{obs_density()}}.
Check the child .Rmd to figure match the arguments to expected name in template.}
}
\value{
A list of environments with length equal to the number of UPL
calculations used in report
}
\description{
Takes a data set and variable lists to loop through and create a list of
self-contained environments that the child.Rmd documents can loop through for
the reg text document templates.
}
