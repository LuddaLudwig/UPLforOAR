% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/output_likelihoodGroup.R
\name{output_likelihoodGroup}
\alias{output_likelihoodGroup}
\title{Organizes mcmc output from \code{\link[=run_likelihoodGroup]{run_likelihoodGroup()}} with a hierarchical group structure}
\usage{
output_likelihoodGroup(jags_model_run, significance = 0.99)
}
\arguments{
\item{jags_model_run}{The output list returned from \code{\link[=run_likelihoodGroup]{run_likelihoodGroup()}},
which includes the jags model \code{run_results}, likelihood distribution type,
and \code{data}, \code{xvals}, and \code{future_runs} used as inputs to \code{\link[=run_likelihoodGroup]{run_likelihoodGroup()}}.}

\item{significance}{Level of significance from 0 to 1, the default is \code{0.99}.}
}
\value{
A list including \code{distr}, the distribution used in \code{\link[=write_likelihoodGroup]{write_likelihoodGroup()}},
\code{predicted_mean}, the mean of the fitted distribution, \code{UPL_Bayes}, the
upper predictive limit based on the \code{significance} level and average
distribution of \code{future_runs} number of draws, \code{obs_pdf}, the predicted
probability density at each observation, and \code{pred_pdf}, the predicted
probability density at each point in \code{xvals}, and the RNG.state for record keeping.
}
\description{
Output_likelihood() takes the \code{jags_model_run} produced by \code{\link[=run_likelihoodGroup]{run_likelihoodGroup()}},
merges the mcmc chains and calculates the UPL as well as
providing the predicted pdf and metrics. Rather than
independent runs defining the population distribution, hierarchical
dependency within groups is allowed with the
group-level distribution parameters drawn from the overall population distribution.
}
