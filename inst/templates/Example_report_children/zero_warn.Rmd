

```{r echo = FALSE, appendix = FALSE, eval = zero_warn, results = 'asis'}
cat('Warning: the data for HAP ', HAP, ' contain emissions with a value of 
    zero. These have been removed from the data set prior to analysis, but 
    consider checking for errors or proper significant digit handling.')
